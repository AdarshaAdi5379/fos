{
  "name": "unbound-config",
  "version": "1.0.0",
  "description": "Configuration files for Unbound platform",
  "main": "environment.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}